import { Injectable } from '@angular/core';
import { Reconstructor } from './reconstructor';

@Injectable()
export class ReconstructorService extends Reconstructor {
    constructor() {
        super();
    }
}
